import { Section } from '@/components/ui/section';

export function ContactProcess() {
  return (
    <Section>
      <div className="card">
        <h2>Как стартуем</h2>
        <p className="muted" style={{ marginBottom: 0 }}>
          Бриф → уточнение задачи → подготовка → съёмка → монтаж и пост → правки → финальные версии.
        </p>
      </div>
    </Section>
  );
}
